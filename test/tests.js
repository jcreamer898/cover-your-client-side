"use strict";

module("zip code searcher");

// "should be a zip code search view model"
// "should have fields for zip, city, state, county"
// "should know when a zip is found"
// "should validate zip codes"
// "should check the length of zip codes"
// "should search for zip codes if a zip is valid"
// "should not search for zip codes if a zip is invalid"
// "should set the city state and county with a valid zip"
// "should be able to clear the results"